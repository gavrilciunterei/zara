import React from "react";

const TotalTab = ({ text }) => {
  return (
    <div className="bg-blue-500 p-1 rounded-xl mr-2">
      <p className="text-white font-bold">{text}</p>
    </div>
  );
};

export default TotalTab;
